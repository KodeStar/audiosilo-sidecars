bin/
data/
.DS_Store

# The frontend build. web/dist is generated by scripts/build-web.sh; it is then
# synced into internal/web/dist (embedded into the binary via -tags embedui).
# Neither is committed - a checked-in placeholder under
# internal/web/dist-placeholder/ keeps the default `go build` working with no
# Node toolchain.
web/node_modules/
web/dist/
internal/web/dist/
