import { EmptyStatePanel } from './EmptyStatePanel';

export function LibraryPanel() {
  return (
    <EmptyStatePanel title="Library">
      Folder scan, coverage check against meta.audiosilo.app, and book selection land in M1.
    </EmptyStatePanel>
  );
}
